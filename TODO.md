# DnD-VTT Tasks TODO

## Bugs

## Security

### Backend

### Infrastructure

* Поставить Cloudflare перед сервером
  * Скрывает реальный IP от атакующих
  * Бесплатный тариф закрывает объёмные DDoS атаки
  * Включить "Under Attack Mode" при необходимости

* Ограничить количество одновременных WS соединений с одного IP
  * Настроить в nginx/caddy: не более 10 соединений с одного IP
  * Защита от исчерпания дескрипторов файлов

* Добавить протокол p2p обмена файлами
  * Если Redis станет узким местом из-за файлового трафика - мигрировать FileChunk на WebRTC DataChannel:
    - Сигналинг (SDP offer/answer, ICE кандидаты) остаётся через существующий WS
    - Сами чанки летят напрямую между браузерами, backend не видит файловый трафик
    - Остальной протокол (FileAnnounce, FileRequest, FileAbort) не меняется
    - Для NAT traversal понадобится STUN сервер (бесплатный Google: stun.l.google.com:19302)
    - При симметричном NAT потребуется TURN сервер (платный, coturn self-hosted)

## New Features

### Frontend

* Добавить пешки на сцену
  * Добавить структуру Token в shared: id, name, image_hash, position (x, y в клетках), size (1x1, 2x2 и т.д.)
  * Добавить список пешек в структуру Scene
  * Добавить событие TokenMove { token_id, x, y } - синхронизируется через RoomState
  * Добавить боковую панель с библиотекой пешек (хранится в IndexedDB, не в RoomState)
  * Добавить drag-and-drop из панели на поле сцены для размещения пешки
  * Добавить создание новой пешки: название + картинка через p2p протокол файлов
  * Добавить редактирование и удаление пешек
  * Пешки не видны, если сцена не активная

* Добавить таб "Заметки"
  * Добавить новые типы событий в shared: NotePayload, NoteVisibility (Public, Private, Direct(username))
  * Публичные заметки — хранятся в RoomState, синхронизируются автоматически, редактирует только автор
  * Приватные заметки — хранятся в IndexedDB пользователя, не попадают в RoomState и WS
  * Адресные заметки — доставляются через WS конкретному получателю, хранятся в его IndexedDB
  * Рендеринг Markdown через pulldown-cmark + санитизация HTML через ammonia (защита от XSS)
  * Указывать автора заметки (username) для избежания путаницы
  * Добавить возможность перетащить заметку из окна с заметками на поле и передвигать её куда угодно, 
  при этом её видят в зависимости от видимости 

* Добавить сжатие и шифрование для тела AppState
  * Сжатие через deflate/gzip на клиенте перед отправкой WS snapshot (js_sys или flate2 с wasm фичей)
  * Шифрование — симметричное, ключ деривируется из названия комнаты + общего секрета участников
  * Backend остается дumb relay — не знает о сжатии и шифровании

* Добавить тулинг для удобства использования доски (добавить кнопки в правый нижний угол над подсказкой как управлять камерой)
  * Линейка, которая правильно по правилам dnd может измерить расстояние в 
  дюймах относительно выбранной сцены (с учётом размера клетки в сцене) (работает только в рамках сцены)
  * Указатель как в миро, оставляющий след за перемещением (работает глобально)
  * Добавить комбинацию alt+lbm для уведомлений в выбранной точке, чтобы привлечь внимание, как в доте
  * Добавить возможность отправления сообщений конкретному пользователю через "@ник <сообщение>" доставляются через WS конкретному получателю, хранятся в его IndexedDB